# 搜索与棋手页交互体验 Review(2026-07-12)

> 范围:`docs/app.js`(2,018 行)中搜索(`renderSearch`/`searchPlayers`)与棋手详情(`renderDetail`/`renderDomesticPlayerDetail`)的实际实现。只提意见,不改代码。所有问题均标注代码依据。
> 评审视角:棋童家长的典型旅程——打开 → 搜孩子 → 从同名中认出 → 看档案 → 复盘 → 转发到家长群。

## 〇、总评

交互骨架是对的:深链、消歧卡片、三态徽标、棋盘键盘操作、缺口登记、隐私请求入口,这些设计在同类静态站点里属于上乘。但沿家长旅程走一遍会发现,**旅程的第一步(等搜索可用)和最后一步(转发分享)都是断的,中间还有两处"看得见却点不动"**:浏览器返回键会直接把人送出站,赛事记录只显示 12 条且无法展开。以下按旅程顺序展开。

## 一、搜索体验

### S1(高)全站没有历史记录,浏览器返回键 = 离开网站

路由只用 `history.replaceState`(app.js:1653),从看板→搜索→棋手→赛事的每一步都不产生历史条目。家长在微信/手机浏览器里最本能的操作是按返回键回上一步,现在按一下直接退出网站。`popstate` 监听(app.js:320)写了,但没有 pushState 可回退,形同虚设。这是搜索和棋手页共享的第一大断点。建议:选择进入棋手/赛事时 pushState,返回键回到上一视图(搜索词和滚动位置一并恢复)。

### S2(高)搜索可用之前,要先下完约 18 MB JSON——且没有任何等待反馈

`loadData()`(app.js:83)在首帧就拉取 registry 4.3MB + domestic 搜索索引 9.1MB + youth-leaderboards 2.7MB + by-player 1.7MB 等全部数据,搜索必须等 `players` 数组建好才有结果。弱网手机上会出现长达十几秒的窗口:**搜索框能输入,但敲什么都没反应,也没有"数据加载中"的提示**——家长的结论会是"这网站是坏的"。最低成本的呈现层改进:数据就绪前搜索框显示加载态(placeholder 改为"正在加载 2.6 万名棋手…"),就绪后再启用;中期方向:搜索索引分片按需加载、首屏只拉排行榜所需数据(前 4 个请求目前还是串行 await,app.js:85-88,白白叠加延迟)。

### S3(高)结果被静默截断在 30 条,计数还撒了谎

`searchPlayers` 固定 `slice(0, 30)`(app.js:1825),而结果头部显示的"N 条档案"用的是截断后的 `matches.length`(app.js:576)。搜一个常见姓氏,实际命中几千条,界面显示"30 条档案"且无任何"仅显示前 30 条,请补充拼音/出生年缩小范围"的提示——家长搜孩子没搜到,不会想到是被截断了,只会认为库里没有,然后流失(而且不会触发缺口登记,因为有结果时空态不出现)。**截断可以,不透明不行。**

### S4(中)输入一个字,正在看的棋手页就没了

`renderSearch` 里 `hasQuery` 直接隐藏 detailPane/eventPane(app.js:562-563)。家长正在看孩子档案,想顺手查对手,在搜索框敲第一个字母的瞬间当前页面消失;清空搜索也回不来(状态还在,但心理上是"闪没了")。建议:搜索结果以下拉浮层呈现而非整页接管,或至少在清空查询时恢复原视图并保留滚动位置。同根问题:棋手页"← 返回"固定回看板(`back-to-dashboard`),从搜索进来的用户丢失搜索现场。

### S5(中)结果排序把赛事组放在棋手前面,且无匹配高亮

拼接顺序是 `${eventResults}${playerResults}`(app.js:592)。家长搜的是名字,首屏却先看到赛事列表;棋手才是本站主实体,应排在前(或按查询形态动态排序:纯数字/含"杯赛锦标"字样时赛事优先)。另外匹配词在结果中无高亮,同名消歧卡与单人卡混排时,家长需要逐条读完才能定位差异——把命中的拼音/汉字加粗,扫视成本立减。

### S6(中)无键盘路径、无防抖、无输入辅助

方向键不能在结果间移动、回车不能选中首条、无 "/" 快捷聚焦(棋盘有键盘支持,搜索反而没有);`input` 事件每键触发全量 2.6 万实体评分(app.js:251,无 debounce、未处理中文输入法 compositionstart/end,组字过程中每个键都跑一遍全库扫描并闪动结果);无最近搜索/最近查看(家长反复查同几个孩子,这是最高频路径);空搜索框无示例引导(可展示"试试:搜索你孩子的名字"+ 一个真实样例)。单字符查询即全库扫描,建议 ≥2 字符再触发。

### S7(低)计数文案口径混乱

同一屏出现"3 个姓名 · 2 项赛事"(app.js:564)和"5 条档案"(app.js:576),"姓名/档案/实体"三种口径并存。家长只需要一种:"找到 N 位棋手(其中 M 个名字下有多条待区分档案)"。

### 值得保留的亮点

未命中空态是全站最好的交互之一:引导登记缺口 + "只在本机记录、不会静默上传姓名"的透明承诺(app.js:593);消歧卡片"同名待区分 + 逐条区分特征 + 解释文案"的结构正确——它的问题不在交互而在数据(publicLocation 未落地导致无 FIDE 分支的区分特征缺失,见 PM_REVIEW_2 N1)。

## 二、棋手页体验

### P1(高)赛事记录只渲染 12 条,没有"查看更多"

`playerEventHistory` 硬切 `slice(0, 12)`(app.js:1097),标题却显示真实总数(如"45 项")。活跃棋手的早期赛事完全不可达——不是折叠,是不存在任何交互能看到。对想回顾孩子完整成长轨迹的家长,这是功能性缺失。同时列表无排序/筛选(按时间倒序?按有无棋谱?),名次信息(`rank`)存在但埋在 small 字体里——名次恰恰是家长最先找的字段,应提为行内主信息。

### P2(高)深链已经支持,但页面上没有"分享"

URL 带 `?fideID=` 可直达棋手页,而页面动作区只有"返回 / FIDE 主页 / 补充或勘误"(app.js:666-671)——没有复制链接或分享按钮。家长群转发是本项目唯一现实的自然增长通道(此前两份报告的共同判断),而微信 webview 里用户连地址栏都看不到,等于分享通道为零。一个"复制本页链接"按钮(带预制文案,如"××的国际象棋档案:N 盘对局、最新等级分 ×××")是全站投入产出比最高的单个交互。

### P3(中)工程术语直接说给家长听

"本地青少年 bulk 命中 27 盘"(app.js:1066)、"按棋手聚合静态包"(app.js:1089)、"查询到该棋手在本库中的棋局数量"——这些是维护者的语言,不是家长的语言。对比之下"查询到该棋手…"已经算好的了。建议建立一层面向家长的文案规范:"已收录他/她的 27 盘对局(来自青少年赛事直播)""全部对局 / 8 岁组 / 10 岁组"替代 U8/U10 裸标签(或首次出现时加注释)。文案层是零风险改动,却直接决定家长对专业度的感知。

### P4(中)等级分只有三个静态数字,没有走势

rating 卡(app.js:676-680)只显示当前 standard/rapid/blitz。家长打开档案的第一问题是"涨了没有"。数据侧已有联邦月度快照机制的雏形,把每月 standard 分存成序列后,一条 sparkline 就能回答这个问题——建议列入路线图,并在卡片上先加"较上月 ↑12"这种最小形态。

### P5(中)棋盘的键盘/自动播放功能藏得太深

← → 翻步、空格自动播放(app.js:1462-1477)做了,但界面上没有任何提示,移动端也无手势说明。建议棋盘下方加一行浅色提示("← → 翻看着法,空格自动播放"),移动端首次打开时短暂浮层。已有的能力不让用户知道,等于没做。

### P6(中)无 FIDE 棋手页:关键区分信息全是"未公开/待补"

`renderDomesticPlayerDetail` 的三格摘要(赛事记录/参赛组别/公开地区,app.js:724-728)在当前数据下,"公开地区"因 publicLocation 未重建全部显示"未公开",组别常为"待补"——家长面对 5 个同名档案时,页面提供不了任何认亲依据。交互框架是好的,被数据拖累(数据问题见 PM_REVIEW_2 N1,重建后即恢复)。另外:"补充身份线索"和"删除/匿名化请求"并列很好,但缺少家长第一动作的显性入口——**"这是我的孩子"认领按钮**(会诊方案 3.3 节设计),目前认领意图只能绕道"补充身份线索"表达,转化必然折损。

### P7(低)两处不一致

隐私请求入口只有无 FIDE 页有,FIDE 棋手页没有(未成年 FIDE 棋手同样应有);贡献入口文案 FIDE 页叫"补充或勘误"、domestic 页叫"补充身份线索",建议统一动词结构。

### P8(低)区块顺序不符合家长的阅读优先级

当前顺序:评级卡 → 棋局包 → 覆盖状态 → 赛事记录 → 同名记录 → 棋盘。家长旅程是"确认身份 → 看最近比赛成绩 → 复盘"。建议:身份区(现有,好)→ **最近 3 场赛事+名次**(新增摘要行)→ 复盘入口 → 等级分 → 完整赛事记录 → 覆盖状态/同名(信任信息沉底)。覆盖状态和同名提示是"数据库的自我说明",不应插在家长要找的内容中间。

## 三、优先级汇总

| 级别 | 事项 | 一句话 |
|---|---|---|
| 高 | S1 pushState 历史栈 | 返回键不再把家长送出网站 |
| 高 | S2 搜索加载态 + 索引瘦身 | 搜索框在数据就绪前要"会说话" |
| 高 | S3 截断透明化 | "仅显示前 30 条 + 缩小范围引导" |
| 高 | P1 赛事记录可展开 | 12 条之外的记录现在完全不可达 |
| 高 | P2 复制/分享按钮 | 打通家长群传播,全站 ROI 最高 |
| 中 | S4/S5/S6 搜索浮层化、棋手优先、高亮、防抖、键盘、最近查看 | 搜索从"能用"到"顺手" |
| 中 | P3 家长文案层 | 消灭 "bulk 命中" |
| 中 | P4 等级分走势(先做"较上月±N") | 回答"涨了没有" |
| 中 | P5 棋盘操作提示 | 让已有功能被看见 |
| 中 | P6 认领入口 + 等数据重建恢复区分特征 | 消歧的最后一公里 |
| 低 | S7/P7/P8 口径、文案与区块顺序统一 | 细节打磨 |

一个共同主题:这套前端的**能力**(深链、消歧、键盘、缓存、乱码修复)明显强于它的**表达**——大量已实现的价值因为缺一行提示、一个按钮、一条历史记录而没有到达用户。下一轮迭代不需要新能力,需要把已有能力说出来、连起来。
