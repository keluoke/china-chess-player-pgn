# 整体 Re-review(2026-07-12)

> 对象:main @ 20136294(上次 review 后新增 3 个提交)。只评审、不改码。
> 对照基线:PM_REVIEW.md(2026-07-11)与 DATA_PRODUCT_PLAN.md 会诊报告。

## 〇、总评

三个提交(`8e39eb33` 国内赛事整取、`b7671d4d` 指标统一与贡献流、`20136294` API manifest 修复)精准命中了前两份报告的多数 P0/P1 建议,执行质量高于预期——不是打补丁式响应,而是把建议消化成了自洽的机制(指标契约、赛事队列、消歧卡片、覆盖页、陪跑计划)。

但项目的主要矛盾已经转移:**从"机制缺失"变成了"机制空转"**。新建的每一块仪表盘读数都是 0 或 1——贡献漏斗全 0、身份链接 0 条、鸣谢 0 人、201 个赛事整取目标只抓了 1 个、快照审计 0。同时出现一个新的 P0:**隐私收敛只做了前端,公开 JSON 里 14,600 条俱乐部/学校全名照旧可下载**,呈现与数据层表里不一。

## 一、上次发现的逐项核销

| 上次发现 | 状态 | 实测证据 |
|---|---|---|
| P0-3 三处指标打架 | ✅ **彻底解决** | `public-metrics.json` 单一契约 + `PUBLIC_METRICS.md` 口径文档;index/API/changelog/dashboard 四处实测一致(2,589 人 / 72,610 局);CI 跑 `validate_public_metrics.py` 强制一致;旧 direct 口径降级为 `sourceTotals` 仅供诊断——处理得比建议更干净 |
| P0-2 贡献通路 GB 级下载 | ✅ **大体解决** | 工具源码(548 行)移出主仓库,独立仓库 `china-chess-contributor` 已验证存在且有 v1.1.0 Release(6 个免 Python 打包产物);网页向导升级为设备码直开结构化 Issue,不再"复制 JSON 到微信";文档同步更新 |
| 冷启动策略缺失 | ✅ 已立项 | `CONTRIBUTOR_PILOT.md`:3–5 人定向陪跑、48 小时反馈、复盘漏斗;并明确"不得预填虚假贡献者、不得以鸣谢换取未成年人隐私"——两条红线加得好 |
| 空鸣谢栏目负面信号 | ✅ 已解决 | 无贡献时整个区块隐藏 |
| 同名实体平铺伤信任 | ✅ 已实现 | 消歧卡片(同名折叠 + 逐条区分特征)+ 三态语言(已核验/待确认/同名待区分),与会诊方案的设计一致;14,611 实体已划分为 same-name 9,829 / pending 4,782 |
| 需求驱动缺口队列 | ✅ 已实现 | 搜索未命中 → "登记这条数据缺口"入口(本机记录、明示不静默上传姓名)+ `data-demand-gaps.csv` |
| 赛事整取优先 | ✅ 机制就绪 | `build_domestic_event_queue`:201 个目标(李成智杯各届各组优先,带优先级分) |
| 覆盖与信任基建 | ✅ 已上线 | `coverage.html`:总覆盖 22.27%、分年龄段覆盖条、整取队列、质量审计(21,091 赛事扫出 90 条异常,含此前发现的未来日期类问题)、贡献漏斗 |
| 置信分无区分度 | ⚠️ 半成 | 证据加权已实现(俱乐部一致 +20、跨赛事等),但唯一证据源仍是单一 starting-rank,实测全员仍为 low(0 分 9,829 人、20 分 4,782 人)——模型好了,原料还没进来 |
| P0-1 pages.dev 可达性 | ❌ **未动** | 仍无自定义域名;且新贡献主路径依赖 github.com 设备码授权,GitHub 在大陆同样不稳,贡献通路的可达性风险实际上加倍了 |
| 站点访问统计 | ❌ 未动 | 漏斗只覆盖贡献侧;访问侧仍然盲飞 |
| SEO/OG/sitemap、bulk 出 git、文件数预算 | ❌ 未动 | 维持 P2 |

## 二、新发现

### N1(P0)隐私收敛表里不一

前端已经很克制:消歧卡片与档案页只显示"公开地区"(省市级)、年龄段标签替代出生年,`sync_domestic_players.py` 也新增了 `public_location()`(俱乐部文本→省市)。但两处脱节:

1. **线上 `docs/data/registry/domestic/players.json`(公开可直接下载)仍完整携带 14,600 条 `club` 全名和 sighting 明细**——前端不显示≠数据不公开,任何人一个 URL 就能拿到全部未成年人"姓名+俱乐部/学校+参赛轨迹"的原始表;
2. 该 JSON 尚未按新脚本重建:`publicLocation` 字段实测 0 条,而前端已在读 `player.publicLocation`——字段落空,消歧卡片无 FIDE 分支丢失地区上下文,恰好削弱了消歧能力。

建议:公开产物改为**字段白名单**制——构建期完成 club→publicLocation 折叠,完整俱乐部信息只留在 `data/manual/` 源层(仓库虽公开,但至少不进 API/索引分发面;长期可评估源层脱敏);并把"公开面不得出现 club 原文"写进 CI 校验。当前状态下,做过的前端收敛给了一种已经保护的错觉,比不做更危险。

### N2(P1)死文件:双击即报错的入口还在仓库里

`贡献工具-双击启动.command/.bat` 仍在根目录,`exec python3 Scripts/contrib/contrib_tool.py`——该文件已删除,双击必报错;`一键抓取.app`、`Scripts/contrib/__pycache__` 同样残留。老 README/zip 的用户会踩坑,且根目录的中文启动器会持续误导新用户走已废弃路径。建议清理或替换为一个指向新 Release 页的说明文件。

### N3(P1)"纯静态零运行时"原则已被突破,但架构文档未承认

`functions/api/github/*` 是 Cloudflare Pages Functions——项目第一个运行时组件。实现本身安全(设备码流无 client secret、有 device_code 格式校验、no-store),但:① ARCHITECTURE/README 仍宣称纯静态,应正式声明这个例外及其边界(无状态、只做 GitHub 设备码代理、永不接触数据);② 代理无任何速率限制,可被第三方当作匿名 GitHub API 中转刷额度(低危,但值得加简单限流或 Turnstile)。原则的价值在于被显式管理,而不是被悄悄打破。

### N4(P0,性质变化)管线建成,数据空转——瓶颈已从设计转向执行

所有新机制的实测读数:贡献漏斗 5 项全 0;`player-identity-links.csv` 0 条;`contributors.csv` 0 人;赛事队列 201 个目标、已抓 1、快照审计 0;置信度 100% low。这不再是工程问题——**代码侧已经没有阻塞项,阻塞项是维护者的执行动作**:本地跑队列抓取、发出陪跑邀请、绑定域名。再迭代任何新机制的边际收益,都低于把现有仪表盘上任何一个 0 变成非 0。

### N5(P2)提交卫生

新 3 个提交的 author 是 `test`,鸣谢与贡献统计以 git log 为源之一时会产生噪音。

## 三、更新后的优先级

| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | 公开数据面字段白名单(N1) | 重建 domestic registry,club 不出公开面,publicLocation 落地 + CI 钉死 |
| P0 | 执行周:消化赛事队列首批 20 个目标 + 发出陪跑邀请(N4) | 让漏斗、身份链接、鸣谢至少一项非 0 |
| P0 | 自定义域名 + 大陆可达性实测(遗留) | 现在连贡献流都压在 github.com 可达性上,更紧迫了 |
| P1 | 清理死启动器文件(N2) | 半小时的事,防止第一批真实用户踩坑 |
| P1 | 架构文档声明 Functions 例外 + 代理限流(N3) | |
| P1 | 站点访问统计(遗留) | 覆盖页做了"数据侧仪表盘",还缺"用户侧仪表盘" |
| P2 | SEO/OG/sitemap、bulk 迁出 git、提交 author 卫生 | 维持原判 |

## 四、结语

上一轮的判断"基建先行、验证滞后"在本轮只对了一半:基建响应速度和质量超预期,指标契约、消歧呈现、隐私红线条款这些细节甚至比建议走得更远。但验证侧依然是零——项目现在拥有一条设计完善、空无一物的流水线,和一个可能大部分目标用户打不开的入口。接下来最有价值的工作不在仓库里,在仓库外:抓 20 个赛事、发 5 封邀请、绑 1 个域名。唯一必须先于执行的仓库内动作,是把 N1 的隐私缺口补上——在邀请家长进来之前,先确保他们孩子的俱乐部信息不在公开 JSON 里裸奔。
