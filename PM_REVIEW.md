# 产品经理视角深度 Review(2026-07-11)

> 只评审、不改码。基于仓库当前状态(main @ d9add711)的事实核查 + 产品判断。

## 一、一句话结论

工程底座的成熟度远超项目年龄(git 历史仅 5 天):抓取/索引分离、静态 API v1、治理文档、贡献工具、CI 甄别都已就位。但产品侧存在三个足以让这一切白费的缺口——**目标用户可能访问不到网站、贡献冷启动通路实际走不通、数据可信度的呈现自相矛盾**。当前最大风险不是"做得不够多",而是"基建先行、验证滞后":还没有任何证据表明有真实用户在用它(无统计、贡献者名录为空)。

## 二、项目快照(事实基线)

| 维度 | 现状 |
|---|---|
| FIDE 注册表 | 11,623 名 CHN 棋手;中文名覆盖 3,741(32%);出生年覆盖 11,248(97%) |
| 对局覆盖 | 有对局数据棋手约 1,600–2,600 人(不同口径,见 4.3),占注册表 14%–22% |
| 国内临时身份 | 14,611 个实体 ↔ 14,611 条 sighting,**恰好 1:1,尚无任何合并**;示例实体置信度 low、4 个同名冲突 |
| 社区贡献 | `contributors.csv` 只有表头,**贡献者 = 0** |
| 前端 | 单文件 `app.js` 2,018 行 / 106 个函数;有 query-string 深链、移动端适配、PGN 播放器 |
| 部署体积 | `docs/data` 1.6 GB(bulk 836M + pgn 481M + registry 132M + index 90M),14,775 个文件,全部随 git 与 Pages 分发 |
| 可观测性 | 无任何访问统计、无 SEO 基础设施(sitemap / robots / OG 标签均缺失) |

## 三、定位评审:为谁解决什么问题?

README 和首页都在描述"这是什么"(数据库、排行榜、API),但没有回答"**谁**在什么场景下**为什么**要来"。从数据结构反推,真实用户画像有四类,优先级应当是:

1. **棋童家长/教练**(最大盘子):核心任务是"查我孩子/学生的成绩、等级分轨迹、对手是谁、棋谱能不能复盘"。这类人也是唯一现实的贡献者来源(CONTRIBUTING 里"抓自己孩子参加过的赛事"的引导是对的)。
2. **青少年棋手本人**:看排名、看自己和同龄对手的棋。
3. **研究者/自媒体**:排行榜、转会、成长曲线等内容素材。
4. **外部开发者**(风格引擎等):API 消费方,量极小但已被 API v1 服务得很好。

**问题:首页信息架构服务的是画像 3,不是画像 1。** 首页主体是排行榜 + 数据看板("data atlas"叙事),而家长的第一动作是搜索一个具体名字。搜索框在顶栏,但没有引导语、没有"试试搜索:侯逸凡 / 8657238"这类示例、没有空态教育。建议首屏以"查一名棋手"为第一任务重排,排行榜降为第二区块;看板(数据总量、鸣谢)移到页尾——它是给贡献者和自己看的,不是给新访客看的。

另外缺一句 UVP。建议明确写出并贯穿首页/README:"**唯一能用中文名查到中国棋手全量赛事记录和可复盘棋谱的地方**"——这是相对 chess-results(无中文、按赛事组织)和 FIDE(只有分数)的真实差异化。

## 四、关键发现(按严重度)

### P0-1 可达性:目标用户在中国,站点在 pages.dev

唯一线上出口是 Cloudflare Pages 的 `*.pages.dev` 域名。该域名在中国大陆长期处于不稳定/被阻断状态。**画像 1/2/3 几乎 100% 在大陆**,这意味着产品可能对绝大多数目标用户根本打不开——其他一切改进都排在这个之后。

建议(按投入递增):① 先绑定自定义域名(Cloudflare 免费支持,`.com`/`.org` 裸域比 pages.dev 存活率高得多);② 观测真实可达性(找几位大陆家长实测);③ 若确认不可达,评估国内镜像(数据是静态文件,镜像成本低;但需权衡备案成本)。在可达性确认前,**不建议做任何推广动作**。

### P0-2 贡献冷启动:通路设计与"门槛尽量低"目标自相矛盾

治理文档把"贡献门槛尽量低"列为目标,但实际通路是:

1. CONTRIBUTING 第一步是"下载仓库 zip"——仓库含 1.6 GB 的 `docs/data`,这个 zip 体积对家长用户是劝退级的;
2. 第二步要求已安装 Python 3——画像 1 的命中率很低;
3. 网页向导的产出是"复制 JSON 到微信/邮件"——普通用户不理解自己在传什么,维护者收到后还要人肉搬运。

`contributors.csv` 为空印证了通路尚未跑通。建议:

- **把贡献工具从主仓库拆出**:独立小仓库或 GitHub Release 附件(几十 KB 的单文件工具),甚至 PyInstaller 打包免 Python;下载体积从 GB 级降到 MB 级;
- **网页向导升级为第一贡献入口**:向导直接经 GitHub API(设备码流已实现)开 Issue,而不是让用户自己粘贴 JSON 去微信;
- **种子贡献者策略**:冷启动不靠自然流量,靠维护者手工邀请 3–5 位棋院教练/活跃家长,陪跑完成首批贡献,把鸣谢名录从 0 变成非 0(空鸣谢栏目本身是负面信号,建议在有真实贡献前先隐藏该区块);
- 度量贡献漏斗:工具下载数 → 抓取成功数 → PR 数 → 入库数,否则无法判断卡点在哪一步。

### P0-3 数据可信度:同一天生成的三处指标互相矛盾

| 数据面 | 有对局棋手 | 总对局 |
|---|---|---|
| `docs/data/index/manifest.json` | 1,757 | 27,064 |
| `docs/api/v1/manifest.json` | 1,596 | 25,421 |
| `docs/data/changelog.json` | 2,589 | 72,610 |

三者生成时间都在 7 月 9 日,却给出三套数字且均未标注口径(推测 changelog 含了 bulk、API 口径更严,但用户无从得知)。对一个把"数据正确性优先"写进治理第一条的项目,**对外指标打架是最伤信任的事**——尤其 API 消费者会拿 manifest 当契约。建议:定义唯一指标口径文档(什么算"有对局"、bulk 是否计入),所有构建脚本从同一个统计模块取数,并在展示处标注口径;把"三处 totals 一致"加入 CI 校验。

### P1-1 国内临时身份层:1.5 万个"半成品实体"的呈现风险

14,611 个 domestic 实体与 sighting 严格 1:1,说明身份合并(identity link)机制虽已设计但从未运转(`player-identity-links.csv` 也是空的)。这些实体大量同名(示例"丁一"有 4 个同名冲突)、置信度 low、`reviewRequired: true`,却已进入搜索结果。普通家长搜孩子名字看到 5 个分裂的同名条目,得到的印象是"这个库不准"。

建议:① 前端对 unlinked/low 实体明确降权和标注(如"待核实"徽标 + 一行解释),同名簇折叠展示;② 承认人工逐条 review 1.5 万条不现实,先做**批量启发式预合并**(同名+同俱乐部+同赛事序列)生成候选队列,人工只裁决候选;③ 把"认领/合并你孩子的记录"设计成家长的第一个贡献动作——身份合并恰恰是只有家长掌握 ground truth 的任务,这是数据需求与贡献冷启动的天然结合点。

### P1-2 增长基础设施缺失:SEO 与分享

11,623 个棋手页是天然的长尾 SEO 资产("<棋手名> 国际象棋"这类搜索几乎无竞争),但当前是纯客户端渲染 SPA:搜索引擎抓到的是空壳,微信/QQ 分享无卡片(无 OG 标签),无 sitemap/robots。深链(query 参数)已经有了,只差让外部世界看见。

建议(纯构建期即可,不破坏"零运行时"原则):构建时为每位有数据的棋手生成极简静态 HTML(标题 + 元描述 + OG 标签 + 跳转/内嵌 SPA),生成 sitemap.xml 与 robots.txt。这可能是投入产出比最高的单项增长动作。同时补一个隐私友好的统计(如自托管 Plausible 或 Cloudflare Web Analytics),结束"盲飞"状态。

### P1-3 合规:未成年人个人信息

库的核心内容是**未成年人**的实名 + 出生年 + 俱乐部/学校 + 参赛轨迹,聚合呈现并以 CC BY 4.0 鼓励再分发。数据虽源自公开页面,但"公开可查"≠"可聚合再分发",且《个人信息保护法》对未成年人信息有单独的敏感级要求。现有文档只讨论了 PGN 的版权边界,未触及人格权/个人信息维度。

建议:① 增加隐私声明页,写明数据来源、用途与边界;② 提供**数据主体删除/匿名化请求通道**(Issue 模板即可,成本极低但姿态关键);③ 重新评估对 domestic 层(纯国内小龄棋手,最敏感)字段的公开粒度,如俱乐部/学校是否需要在前端全量展示;④ CC BY 4.0 数据许可对这部分数据是否适用,建议咨询后在 LICENSE-DATA 里单列。这件事在项目获得关注**之前**做,远比出事后做便宜。

### P2-1 规模墙:1.6 GB / 14,775 文件全在 git 与 Pages 里

Cloudflare Pages 单次部署有 2 万文件、单文件 25 MiB 的硬限制;当前 14,775 个文件已用掉约 3/4 配额,而对局覆盖率才 15%–22%。覆盖率涨到 50% 时会撞墙;git 仓库本身也会因 PGN/zst 二进制无限膨胀,拖慢每一次 clone/Actions checkout,并反过来加重 P0-2 的 zip 体积问题。建议提前规划:bulk 分片(836 MB)迁出 git(R2 / Release assets / 独立数据仓库),per-player 小 JSON 合并为分片大文件,把"文件数/体积预算"写进架构文档。

### P2-2 单点风险

抓取依赖维护者一台住宅 IP 机器,审核依赖维护者一人,bus factor = 1。治理文档已意识到这点,短期无解,但可以:把 `refresh.sh` 的运行状态(上次成功时间)暴露到看板,让数据新鲜度可见;数据超过 N 天未更新时前端提示——把"单点故障"至少变成"可见的单点故障"。

## 五、被低估的亮点(应保持并放大)

抓取/索引/部署三层分离的架构判断正确且文档诚实(明说 chess-results 封数据中心 IP);CI 对贡献载荷的"同款解析器重放比对"甄别设计在同类社区项目里罕见地严谨;证据分层 L1–L5 与同名消歧规则写得清楚;静态 API v1 的冻结承诺、sha256 增量、CORS 全开对开发者非常友好;lichess-pgn-viewer 复盘体验是对家长/棋手的真实价值点。REVAMP_PLAN 中"驳回一人一 YAML、驳回三级信任体系"的两处反过度设计判断,都是对的。

## 六、优先级路线图

| 优先级 | 事项 | 预估投入 | 对应发现 |
|---|---|---|---|
| P0 | 绑定自定义域名 + 大陆真实可达性验证 | 0.5 天 | 4.P0-1 |
| P0 | 接入隐私友好统计,结束盲飞 | 0.5 天 | 4.P1-2 |
| P0 | 统一三处指标口径 + CI 一致性校验 | 1 天 | 4.P0-3 |
| P0 | 贡献工具拆出主仓库,砍掉 GB 级下载 | 1 天 | 4.P0-2 |
| P1 | 隐私声明 + 删除请求通道 + domestic 字段粒度评审 | 1 天 | 4.P1-3 |
| P1 | 首页信息架构重排(搜索优先)+ 低置信度实体标注/折叠 | 1–2 天 | 三、4.P1-1 |
| P1 | 构建期棋手静态页 + sitemap + OG | 1–2 天 | 4.P1-2 |
| P1 | 种子贡献者邀请与陪跑(非工程) | 持续 | 4.P0-2 |
| P2 | domestic 批量预合并候选队列 | 2 天 | 4.P1-1 |
| P2 | bulk 迁出 git、文件数预算 | 1–2 天 | 4.P2-1 |

判断标准建议:**四周内,大陆用户可稳定访问、看板不再指标打架、鸣谢名录出现第一批真实昵称**——这三件事比任何新功能都更能决定项目成败。
