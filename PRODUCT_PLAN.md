# 中国国际象棋棋手数据库完整产品方案

> 文档日期：2026-07-18
>
> 审查范围：当前本地工作树（含尚未提交的棋协大师赛赛果、逐轮对阵和 PGN 产物）
>
> 文档性质：产品与技术方案；不包含代码修改
>
> 顶层约束：以 `AGENTS.md`、`Scripts/local/README.md` 和 registry 权威规则为准

## 1. 结论

项目已经拥有建设“中国棋手最好用的数据入口”所需的三块稀缺资产：全量 FIDE 中国棋手注册表与中文名强制勘误机制、114.6 万盘 Lichess Broadcast 授权棋谱、以及正在快速补齐的国内赛事名单、排名、逐轮赛果和 PGN。问题已不再是“有没有数据”，而是数据还没有收敛成一个可解释、可验证、可扩展的统一产品。

下一阶段不应继续横向增加脚本和页面，而应完成四次收敛：

1. **从多个派生 JSON 收敛为一套规范事实模型**：注册表管身份和当前官方等级分，赛事层管参赛事实，对局层管棋谱，身份链接层只管证据和人工裁决。
2. **从“抓到了”收敛为可验证的完整性状态**：名单、排名、全部轮次、全部配对、结果、PGN 及 PGN—配对匹配必须分别计数；只有全部通过才允许称为完整赛事。
3. **从官方分排行榜扩展为双轨能力**：官方 FIDE 排名保持纯净；参考估分和预测名次作为独立模型产品，展示区间、不确定性、样本和版本，绝不混入官方榜。
4. **从大仓库静态站收敛为轻前端 + 分片 API + 对象存储**：浏览器只取当前查询所需的小文件，大体积 PGN/bulk 走对象存储和 CDN，保证国内网络下也能快速搜索和打开档案。

产品主叙事建议固定为：

> 搜棋手 → 看已核验身份、赛事与对局 → 看官方等级分或参考估分 → 看同年龄段和同出生年位置 → 发现缺口时提交证据。

不建议将产品扩展成综合下棋平台、社交平台或棋风模拟器。搜索、档案、赛事、对局、排名和数据补充已经构成完整且有辨识度的闭环。

## 2. 当前事实基线

以下数据直接读取当前工作树的 manifest、公开索引和生成目录；不同产物的生成时间并不一致，因此它们是审查快照，不是一个原子发布快照。

| 资产 | 当前规模 | 判断 |
| --- | ---: | --- |
| FIDE registry | 11,646 人 | 身份和当前等级分的唯一权威，基础可靠 |
| 有中文名 registry 棋手 | 2,440 人（约 21.0%） | 中文搜索可用，但覆盖仍是长期核心指标 |
| 有出生年 registry 棋手 | 11,271 人（约 96.8%） | 足以建立官方年龄榜和出生年榜 |
| 有任一官方等级分 | 4,109 人 | 其余不能被简单称为“低分”，只能称“当前无官方分” |
| 国内无 FIDE 临时实体 | 14,676 个 | 全部仍为 unlinked，且当前每个只有一次 sighting |
| Lichess Broadcast bulk | 78 分片、1,146,297 盘 | 最大棋谱底座，必须继续保留 CC BY-SA 4.0 署名与许可 |
| Lichess 青少年投影 | 1,912 人、41,021 盘 | 可直接支撑青少年档案和对局研究 |
| 当前 by-player 聚合 | 2,622 人、73,742 盘、5,780 个包 | 已能支撑档案、分年龄 PGN 和在线播放 |
| 结构化赛事产物 | 933 场 | 884 场标记 `captureStatus=complete`，但该状态只代表结构抓取 |
| 公开赛事详情 | 779 场、52,338 条排名、244,282 条配对 | 国内赛事事实规模已经发生质变 |
| 全赛事 PGN 归档 | 136 场 | 只说明已有整包归档，不能据此把其余赛事都判为抓取失败 |
| 来源 PGN 标记 | 924 场含配对：742 场零标记、179 场部分标记、3 场全部原始配对有标记 | 大多数赛事是来源未发布 PGN；原始口径仍需排除轮空后再判断全台覆盖 |
| PGN—配对本地关联 | 123 场有至少一条；2,180 / 244,282 条配对 | 当前“0 场全匹配”以全部配对为分母，混入轮空和本来未直播台次，不能单独证明抓取失败 |
| 棋协大师赛公开目录 | 153 场 | 已成为国内赛事数据的主力类别 |
| 李成智杯公开目录 | 18 场 | 与目标历史覆盖仍有明显差距 |

### 2.1 已经做对、必须保留的机制

- registry 是姓名、FIDE ID、联邦和当前等级分的唯一权威；派生层不能反向覆盖。
- `data/community/name-corrections.csv` 在构建最后强制应用，并能清除已知错误别名；8602980/8608288 的历史事故已经变成可执行防线。
- `sanitize_person_name`、`validate_registry_authority.py` 和发布前断言形成了正确的“候选可脏、主档必须净”边界。
- 无 FIDE 棋手不因同名、同俱乐部或同年龄组自动合并；`player-identity-links.csv` 是唯一人工合并入口。
- FIDE、Lichess、Chess-Results 机器产物走本地 staging、manifest、单写者分支、云端 ingest/rebuild/deploy 和回执链；原始 HTML 留在仓库外。
- Lichess 授权和署名已写入 bulk manifest；大规模棋谱可被离线重建为按棋手、按自然年龄段的 PGN 包。
- 当前前端已有中文/拼音/FIDE ID 搜索、输入法组字保护、历史栈、深链接、在线棋盘、分享、匿名化入口和移动端折叠，交互基础不需要推倒重来。

### 2.2 目前最危险的错觉

#### “complete” 仍不等于完整赛事，PGN 缺失也不都等于抓取失败

`sync_chess_results_event.py` 在名单、排名和逐轮配对成功后先把赛事加入 `complete_ids`；随后 PGN 阶段即使返回不完整，派生索引仍可能按原来的 `complete_ids` 重建。这确实是发布事务边界错误，但不能再用“884 场结构完成、只有 136 场全赛事 PGN”简单推导其余 748 场抓取失败。

现有抓取器其实已经区分三类底层结果：完整配对页没有任何非轮空 PGN 标记时返回 `source_unavailable / SOURCE_PGN_NOT_PUBLISHED`；访问或解析抛错时返回 `error`；端点返回空内容时返回 `empty`。问题是这些状态尚未被稳定投影成完整性报告、人工补件队列和前台文案。

当前 933 场详情中有 924 场含配对；按尚未排除轮空的原始标记统计，742 场没有任何 PGN 标记，179 场只有部分台次带标记，3 场全部配对带标记。国内棋协大师赛公开组通常只直播部分台次，其他组别经常完全不发布 PGN，因此合理的完整性分母应是“来源实际承诺的非轮空棋谱”，并另行记录它是直播台覆盖还是全台覆盖。

盐城数据可以作为状态建模样例：

| 赛事/组别 | 当前可核验事实 | 判断与后续动作 |
| --- | --- | --- |
| 2025 盐城站棋协大师赛公开组 `tnr1266792` | 315 条配对，306 条带 PGN 标记，整包 306 盘；其余为轮空口径 | `advertised-full` 且 `archived-full-board`，不需要补抓 |
| 2026 盐城站棋协大师赛公开组 `tnr1437536` | 117 条配对，115 条带 PGN 标记，整包 115 盘；其余为轮空口径 | `advertised-full` 且 `archived-full-board`，不需要补抓 |
| 盐城站候补组、一级至四级等已抓组别 | 详情和逐轮配对存在，但 PGN 标记为 0 | `not-published`，属于正常来源缺口，不应自动重试或报抓取失败 |
| 按现有映射对应盐城快棋赛的 `tnr1210265`–`tnr1210272` | 当前没有逐赛事全盘归档；其中 6 个组别留有按棋手 PGN 包，2 个组别尚无本地包；维护者提供了“当时曾发布全台 PGN”的明确线索 | 标为 `locally-recoverable + external-lead` 的 P1 补件任务：先离线合并去重并核对轮次/台次覆盖，再补取或人工上传缺少的 2 个组别；验证前不得宣称当前已全台归档 |

因此，PGN 门禁应在可发布候选形成之前计算，但门禁结果必须是多维的：没有发布 PGN 的赛事可以成为 `results-complete`，前台明确显示“赛果完整，来源未公开棋谱”；只有实际承诺的 PGN 全部归档和匹配后，才可再标记相应的棋谱覆盖范围。任何状态都不得把“来源没有棋谱”伪装成“全赛事棋谱完整”。

#### 国内身份层没有消费新赛果价值

当前 domestic registry 仍是 14,676 个单次 sighting，`rank` 和 `score` 填充为 0，身份链接也是 0；与此同时赛事层已经拥有 52,338 条排名和 244,282 条配对。两者之间缺少稳定的“赛事参赛记录 → 身份观察 → 人工链接候选”投影，导致大量新抓数据尚未变成棋手档案、估分图或跨赛事身份线索。

#### 页面和数据仍违反去来源化契约

`docs/app.js` 仍显示 Chess-Results 名称、外链、“原始成绩”“信源 ID”；`public-events.json`、棋手事件索引和公开赛事详情仍包含 `source`、`url`、`sourceURL`、`sourceRefs` 等字段。Chess-Results 的审计证据应存在私有/维护者层，但不应进入公共对象和前台。Lichess 的 CC BY-SA 署名是另一套规则，不能随去来源化一起删除。

#### 发布物不是一个原子数据快照

当前 registry、API、public metrics、赛事索引和 by-player manifest 的时间戳跨越多天；API v1 仍显示 11,646 人、72,610 盘，而当前 by-player manifest 已是 2,622 人、73,742 盘。用户可能在同一页面看到来自不同构建时点的总量、档案和赛事。

## 3. 产品边界与用户

### 3.1 首要用户

1. 棋手和家长：找人、看成绩、看对局、判断同龄位置、补充或纠错。
2. 教练和棋院：按出生年/年龄组发现棋手、研究对局、观察成长和赛事覆盖。
3. 赛事组织者与数据维护者：核验赛事是否完整、处理身份和中文名候选、补缺。
4. 开发者和研究者：通过稳定、版本化、许可清晰的静态 API 使用数据。

### 3.2 明确不做

- 不提供实时对弈、聊天、好友、论坛或复杂账户体系。
- 不恢复棋风模拟；外部产品通过 API 自行实现。
- 不把参考估分包装成官方等级分，不给低置信未成年人输出确定性名次。
- 不在浏览器端直接访问任何上游来源。
- 不自动合并同名棋手，不从赛事记录反写 registry 姓名或等级分。

## 4. 目标信息架构

### 4.1 首页：唯一主入口是搜索

首屏只保留品牌、一个搜索框、可信度短句和四个导航入口。搜索同时覆盖：

- 已核验中文名、拉丁名、拼音、别名、FIDE ID；
- 国内临时实体；
- 赛事中文名、原始内部别名和稳定赛事 ID；
- 常用筛选语法，如 `2016出生`、`U10 女子`、`棋协大师赛 杭州`。

搜索结果卡必须优先回答“是不是这个人”：身份类型、出生年或自然年龄组、公开地区、最近赛事、同名数量。不要在卡片上堆叠数据来源或内部置信分。

### 4.2 棋手档案

建议按以下顺序组织：

1. **身份头部**：中文名、拉丁名、FIDE/国内实体徽标、身份核验状态、出生年/自然年龄组。
2. **当前水平**：官方标准/快棋/超快棋；无官方分时显示独立的“参考估分”卡。
3. **同龄位置**：同年龄段、同出生年两个维度；显示参照池和区间。
4. **成长曲线**：官方历史等级分与模型历史估分分轨展示。
5. **赛事时间线**：名次、得分、轮次、组别、晋级状态和完整性徽标。
6. **对局研究**：在线棋盘、按赛事/时间控制/年龄段筛选、PGN 下载。
7. **数据说明**：更新时间、覆盖范围、缺失项、模型版本、纠错/隐私入口。

无 FIDE 档案应使用稳定的 `/players/domestic-{id}/` 深链接。身份被人工合并后保留旧 ID 重定向，不能让分享链接失效。

### 4.3 排名中心

排名中心必须有两条永久分离的轨道：

- **官方 FIDE 榜**：只按 registry 中官方等级分排序，允许标准/快棋/超快棋、年龄组、出生年、性别和活跃状态筛选。
- **参考水平榜**：官方有分者和达到发布门槛的模型估分者构成参照池；明确写“非官方”，默认显示百分位和名次区间，不显示伪精确的单一名次。

年龄口径必须显示 `basisDate` / `basisYear`。同一位棋手同时可以属于“2016 年出生”榜和当年 U10 榜，前者是单一出生年，后者是两年年龄段，不能混用。

### 4.4 赛事页

赛事页先展示完整性，再展示内容：

- 赛事元数据、分组、日期、赛制、时间控制；
- 名单与最终排名；
- 逐轮配对和结果；
- PGN 覆盖矩阵和无法匹配原因；
- 棋协大师赛晋级线/晋级结果；
- 每位棋手回到档案的链接。

公共页面不展示 Chess-Results 名称、外链和内部证据 ID。维护者工作台保留完整证据链。Lichess 对局页或下载包保留许可、来源名称和许可 URL。

### 4.5 数据健康与贡献页

面向普通用户只展示可理解的覆盖：注册棋手、中文名、出生年、赛事、严格完整赛事、可播放对局和更新时间。抓取错误码、重试队列、私有 URL、冲突哈希只放维护者面板。

贡献动作保持小而明确：补中文名、身份合并证据、赛事线索、赛果缺口、隐私/删除请求。网页只生成提案，不允许社区客户端自动抓取或上传原始 HTML/PGN。

## 5. 规范数据模型

不要再以某个现有 JSON 作为新的权威表。建议把现有数据投影到以下事实实体，再从事实层生成网页和 API。

| 实体 | 稳定主键 | 关键规则 |
| --- | --- | --- |
| `Person` | `fide:{id}` / `domestic:{uuid}` | registry 字段对 FIDE 人物绝对权威；国内实体只存已审核公开字段 |
| `PersonObservation` | `source + eventID + playerNo` | 一次来源观察，不等于一个新人物；包含原始/清洗名、组别、公开地区 |
| `IdentityLink` | `fromID + toID + revision` | 只接受人工审核；可撤销、可追溯，不覆盖历史观察 |
| `Event` | `event:{uuid}` | 与来源 ID 解耦；多个上游 ID/别名映射到同一 canonical event |
| `Section` | `eventID + sectionCode` | 组别、性别、年龄段、时间控制、轮数独立建模 |
| `Entry` | `sectionID + playerNo` | 名单事实；通过 IdentityLink 指向 Person，不反写 Person |
| `Standing` | `sectionID + playerNo` | 名次、得分、破同分；与名单自然键一致 |
| `Pairing` | `sectionID + round + board + whiteNo + blackNo` | 结果、是否实际开局、弃权原因；不以姓名连接 |
| `Game` | 规范化 PGN 指纹 | PGN、双方、结果、时间控制、许可；可关联多个来源观察 |
| `RatingObservation` | `personID + listDate + control` | 保存月度官方分历史；不能只保存“当前分” |
| `EstimateSnapshot` | `personID + modelVersion + asOf + control` | 后验均值/中位数、区间、样本、图距离和发布状态 |
| `RankSnapshot` | `modelVersion + basisDate + cohort + control` | 官方榜与参考榜分开生成，记录完整参照池 |
| `CompletenessReport` | `sectionID + candidateHash` | 每一门禁的 expected/actual/matched/missing 和原因 |

### 5.1 姓名和身份解决机制

现有机制方向正确，建议整理成五层：

1. **来源观察层**：允许保留脏文本，但只在私有审计或隔离区。
2. **清洗候选层**：必须通过 2–6 汉字、标点、赛事标题、尾逗号等规则；失败项进入 quarantine，不得进入公开别名。
3. **审核别名层**：`player-aliases.csv` 保存已确认中文名/拼音/别名。
4. **强制勘误层**：`name-corrections.csv` 最后应用，并保存错误别名 tombstone，防止旧错名复活。
5. **身份链接层**：解决“这条赛事观察属于谁”，与“这个人的规范姓名是什么”分开。

机器候选排序不应只等待维护者先找到强证据，而应主动生成“可能属于同一人”的弱证据假设。它的职责是缩小人工搜索空间，不是替维护者作最终裁决；任何分数都只能决定审核队列顺序，不能自动落 `IdentityLink`，也不能把未确认候选的赛事和估分合并到公共档案。

新增赛事首先要从 `Entry/Standing/Pairing` 投影出带组别、得分、轮数、俱乐部、比赛日期和公开地区的 `PersonObservation`。现有 domestic sightings 的 `rank/score` 基本为空，使已有的晋级逻辑无法参与候选生成；应先修这条投影，再计算候选边。

建议使用可解释的证据卡，而不是只给一个神秘置信分：

| 证据 | 建议权重 | 约束 |
| --- | ---: | --- |
| 规范化中文名一致或已审核别名一致 | 候选前提 | 常见姓名不能单独形成高置信候选 |
| 俱乐部一致 | +25 | 只在维护者审核面板使用；换俱乐部为中性，不作反证 |
| 低一级组得分率 ≥65%，24 个月内出现在相邻高一级组 | +35 | 按 `一级组 → 候补组 → 公开组` 等显式组别图计算，性别需一致 |
| 时间连续、组别变化合理但未达到晋级阈值 | +15 | 只作辅助证据，不推断正式晋级资格 |
| 已核验出生年或年龄连续 | +20 | 必须区分来源自报与 registry 强事实 |
| 公开省市一致 | +10 | 不能替代个人身份事实 |
| 对手/同批参赛者连续出现 | +10 | 防止只靠单场巧合 |
| 临时国内身份随后对应到唯一 FIDE 身份 | +25 | 仍需人工确认链接 |
| 同一时间同一赛事不同组同时出现 | -100 | 硬冲突，候选边禁止合并 |
| 性别或已核验出生年冲突 | -100 | 硬冲突，候选边禁止合并 |
| 时间上不可能重叠的赛事 | -60 | 进入冲突复核，不自动忽略 |

候选分层可先设为：70 分以上且至少来自两类独立证据为 `suggested-high`；45–69 分为 `suggested-medium`；更低分默认折叠。权重和阈值是待历史样本回测的初始产品参数，不是身份事实。典型证据卡可显示：“张某，2024 杭州一级组 6/9（66.7%）→ 2025 苏州候补组；俱乐部一致；间隔 8 个月；未发现并发参赛或出生年冲突。建议核对并合并。”这正是维护者难以逐个主动发现、但机器很适合提出的线索。

同名簇达到 3 条及以上时仍禁止自动合并，但不应继续停留在“只有 `parent-only`、完全不给建议”。应在簇内生成两两候选图，先删除硬冲突边，再按证据排序展示若干互斥方案；维护者可确认某条边、保持分离或标记“不是同一人”。接受后写入可撤销的 `player-identity-links.csv`，拒绝后写入负链接/tombstone，避免同一错误建议反复出现。候选卡必须保存证据快照和算法版本，保证未来权重变化时仍能解释当时为何建议。

当前 `data/generated/chess-results-player-name-map.csv` 仍含尾逗号、赛事标题和非姓名字符串。它应被视为旧机器候选缓存，不得作为 API/前端数据源；完成有效候选迁移后建议删除或移到仓库外隔离区。

### 5.2 赛事完整性状态机

不要再使用单一 `complete/partial` 承载所有含义。内部报告至少包含：

```text
metadata       expected=1     actual=1
roster         expected=N     actual=N
standings      expected=N     actual=N
rounds         expected=R     actual=R
pairings       expected=P     actual=P
results        expected=P     valid=P
pgnExpected    expected=G     archived=G
pgnMatched     expected=G     matched=G
identity       unresolved=0
```

可用状态建议为：

- `captured`：页面已抓取，尚未通过语义校验；只在私有区。
- `results-complete`：名单、排名、轮次、配对和结果全部通过；只能进入明确隔离的赛果/部分数据投影，仍不等于“完整赛事”。
- `archive-advertised-complete`：来源实际承诺范围内应有的 PGN 全部归档、匹配且指纹去重完成；必须同时展示覆盖范围，部分直播台完成也不等于全赛事完成。
- `archive-full-board`：全部实际开局的非轮空配对均有匹配 PGN；只有这一状态可以通过完整赛事的 PGN 门禁。
- `identity-complete`：参赛记录不存在与 registry 冲突，未决身份已隔离。
- `publishable-complete`：`results-complete + archive-full-board + identity-complete` 全部通过，且公共去来源化/许可投影校验通过。
- `partial` / `quarantined`：不得伪装为完整赛事进入公共完整集。

所有自然键、基线 commit/hash、候选 hash 和云端三方合并结果写入同一 receipt。PGN 抓取返回部分成功时，不得继续用原 `complete_ids` 重建公共索引。

### 5.3 PGN 可用性、覆盖范围与人工补充闭环

PGN 状态必须把“来源有没有发布”和“系统有没有成功归档”拆开：

| 状态 | 判定 | 是否自动重试 |
| --- | --- | --- |
| `not-published` | 完整配对页存在，但所有非轮空配对均无 `hasPGN/pgnURL` | 否；只有出现新的外部线索才重开 |
| `advertised-partial` | 部分非轮空台次标有 PGN，通常是直播台 | 只抓并核对这些已承诺台次 |
| `advertised-full` | 所有非轮空台次均标有 PGN | 是；目标为全台归档 |
| `fetch-failed` | 已发现 PGN 标记或有效端点，但网络、内容或解析失败 | 是；保留错误类别和最后尝试时间 |
| `empty-response` | 应有 PGN 的端点返回空、HTML 或无有效对局 | 是；优先判断端点变化/解析器问题 |
| `archived-advertised-complete` | 已归档并匹配全部来源承诺棋谱 | 否；若承诺范围为直播台，不能写成全台 |
| `archived-full-board` | 全部非轮空配对均有已匹配棋谱 | 否；最高覆盖状态 |
| `locally-recoverable` | 按棋手包或旧发布包可在不访问来源的情况下离线恢复整包 | 先离线恢复，不回抓来源 |
| `external-lead` | 组织者/维护者知道公开下载路径或持有文件，但尚未核验 | 进入人工补件队列，不直接视为已归档 |

每个 section 还必须记录 `pgnCoverageScope = none-published / selected-live-boards / all-non-bye-boards / external-supplement`。`pgnExpected` 的分母是来源实际承诺的非轮空棋局数；另设 `allBoardCoverage` 表示相对全部实际开局的覆盖率，二者不能混成一个百分比。这样既能承认“大部分组别没有 PGN 是正常的”，也能发现“公开组明明有直播 PGN，却只归档了一部分”的真正缺口。

维护者后台生成 PGN 补件队列，至少包含：赛事/组别、赛果完整度、来源状态、非轮空配对数、承诺棋谱数、已归档数、已匹配数、可离线恢复包、最后错误、外部线索、下一动作。优先级为：

1. P0：已标记/已承诺的 PGN 缺失、抓取报错或匹配失败；
2. P1：本地已有按棋手包可恢复，或有可信“曾全台发布”线索的赛事，例如盐城快棋赛；
3. P1：只直播部分台次，但已承诺台次尚未全部归档；
4. P2：来源从未发布 PGN，只有在组织者、棋手或维护者提供可信外部文件/路径后才进入补件，不进行无意义的重复抓取。

产品提供两个补充入口：“提交棋谱下载线索”和“提交 PGN 文件”。线索记录网址、赛事/组别、覆盖范围、发现时间和可分发依据；文件先进入仓库外受控收件区，计算哈希、检查赛事元数据/轮次/结果/重复和许可，再由维护者通过 staging、验证与 release manifest 发布清洗后的 PGN。社区 PR 或网页端不得直接触发来源抓取，也不得把原始 HTML 或未经核验的 PGN 写进公共数据。

### 5.4 原子快照

每次发布生成一个 `snapshotId`，registry、events、games、search、rankings、estimates、metrics 和 API manifest 全部引用它。若任一派生层失败，旧快照继续服务，不允许首页总量先更新、棋手档案后更新。

## 6. 参考估分机制

### 6.1 产品定义

参考估分回答的是“依据本库已核验对局，这名棋手目前大约处于什么水平”，不是 FIDE 等级分预测承诺，也不是官方定级。

必须按标准棋、快棋、超快棋分别建模；时间控制未知的比赛不得混入。年龄只用于动态波动先验和排名分组，不应直接给年轻棋手加分或给成年棋手减分。

### 6.2 数据进入门槛

一盘棋进入模型必须同时满足：

- 双方 Person 身份已核验，或观察到 Person 的链接已经人工确认；
- 结果是 `1-0`、`0-1`、`1/2-1/2`，且不是轮空/未开局弃权；
- 赛事至少达到 `results-complete`；
- 时间控制已知；
- 同一棋局规范化 PGN 指纹或自然键去重；
- registry 对 FIDE 锚点的姓名、身份和等级分仍然压制赛事字段。

只有配对结果、没有 PGN 时，可在有“实际开局”证据的前提下参与结果模型，但必须单独记录证据等级；默认不给与完整 PGN 同等权重。

### 6.3 模型路线

#### v0：不公开估分，只做数据就绪度

先输出每名棋手的有效对局数、不同对手数、与官方有分锚点的图距离、时间控制覆盖和身份状态。它能立刻指导抓取优先级，也能防止在数据结构尚未稳定时发布看似精确的数字。

#### v1：动态、锚定、含平局的 Bradley–Terry/Davidson 模型

用 Elo 尺度表示潜在水平 `theta[p,t]`，胜率核心为：

```text
P(i 胜 j) ≈ 1 / (1 + 10 ^ ((theta[j,t] - theta[i,t]) / 400))
```

平局使用 Davidson 扩展，不把平局粗暴拆成半胜半负。棋手水平按月或季度做随机游走；官方月度等级分是带观测误差的锚点，而不是跨多年固定不动的常数。青少年可有更大的时间波动方差，但基础水平先验不因年龄自动抬高。

当前 registry 只有最新等级分，无法正确锚定历史比赛。实施 v1 前必须增加月度 `RatingObservation` 快照；在历史快照积累不足时，模型只覆盖最近 12 个月，并清楚标注范围。

#### v2：成长曲线

在 v1 经回测稳定后，才增加随时间变化的估分曲线、近期状态和成长速度。不要在 v1 同时引入俱乐部、地域、赛事级别等复杂特征，以免模型难以解释且放大样本偏差。

### 6.4 对外发布门槛

FIDE 现行规则以至少 5 盘对有等级分对手的成绩作为初始等级分发布条件之一；本产品的数据质量和赛事类型更混杂，因此“5 盘”只能作为参考下限，不能照搬成产品门槛。官方规则见 [FIDE Rating Regulations 7.1.4](https://handbook.fide.com/chapter/B022024)。

建议参考估分同时满足：

- 至少 9 盘有效对局、至少 5 名不同对手；
- 到官方锚点的对局图距离不超过 2；
- 身份为人工核验或已确认链接；
- 最近 24 个月有足够样本，不能只靠多年以前比赛；
- 80% 后验区间宽度不超过 300 Elo；
- 模型没有检测到异常结果簇、重复棋局或时间控制混杂；
- 对该年龄/赛事类型的回测达到发布标准。

不满足时只显示“数据不足：还需 X 盘 / 身份待核验 / 尚未连接官方锚点”，不显示点估计。

### 6.5 展示格式

推荐：

```text
参考估分 约 1430
80% 区间 1330–1535
依据：18 盘、11 名对手、最近比赛 2026-07
模型：china-skill-standard-v1.2 · 非官方
```

不要显示“准确率 92%”一类无法解释的营销数字。用户应能打开模型说明，看到数据范围、排除规则、回测结果和版本变更。

## 7. 年龄段与出生年排名预估

### 7.1 两种参照池

- **自然年龄段池**：按 `basisYear - birthYear` 进入 U8/U10/U12/U14/U16/U18/U20/OPEN/S50/S65。
- **出生年池**：例如“2016 年出生”，只包含出生年已核验为 2016 的棋手。

赛事报名组 `eventStage` 不能替代自然年龄段 `naturalStage`；跨级参赛不改变棋手所属出生年或自然年龄段。

参照池还必须固定：联邦/转会口径、性别筛选、时间控制、活跃窗口、是否纳入参考估分者和快照日期。没有这些字段的“全国第 N”不可复现。

### 7.2 官方排名

对有官方分棋手给出精确排序，但必须说明：

- 排名只覆盖当前有该时间控制等级分且满足所选活跃口径的人；
- 同分使用并列名次，不用姓名或 FIDE ID 人为破同分；
- 无出生年者不进入年龄/出生年榜；
- 官方榜不纳入参考估分。

### 7.3 预测排名

对每个发布估分的棋手，从后验分布进行 Monte Carlo 抽样。每次抽样都对整个参照池重新排序，最终输出：

- 预测名次中位数；
- 10%–90% 名次区间；
- 百分位中位数与区间；
- 进入前 10、前 10%、前 25% 的概率。

前台默认展示百分位和名次区间，例如：

```text
U10 参考池：约前 18%–31%
预测名次：第 74–129 / 412
2016 出生参考池：约前 22%–37%
```

当区间过宽时只显示“中上 / 中位 / 数据不足”等分位带，不显示第 N 名。参照池中的官方分和估分人数要分别列出。

### 7.4 验证与发布闸门

用“隐藏已有官方分的棋手”做时间外回测：只用回测截止日前的比赛估计其水平，再与下一期官方分和实际对局结果比较。至少发布以下指标：

- Elo MAE / RMSE；
- 胜和负概率的 log loss；
- 80% 区间真实覆盖率；
- 预测名次区间覆盖率；
- 按年龄、性别、地区、样本量和赛事类型分层后的误差；
- 相比“直接用对手平均分”基线是否有稳定提升。

建议初始闸门：整体 MAE ≤150 Elo、80% 区间覆盖率在 75%–85%、任一主要年龄段无超过整体 1.5 倍的系统误差。未达到时模型留在维护者工作台，不对公众发布。

## 8. 网络与交付架构

### 8.1 保留静态优先，但停止把所有数据绑在页面仓库

当前 `docs/data` 约 1.7 GB，其中 bulk 约 830 MB、PGN 约 488 MB、index 约 224 MB、registry 约 125 MB。Git + Pages 适合站点壳和小型 JSON，不适合长期承载百万棋谱及不断增长的逐赛事产物。

建议：

```text
Pages / 静态站
  HTML、CSS、JS、manifest、小型搜索和排行榜分片

对象存储 + CDN
  bulk zst、按棋手 PGN、全赛事 PGN、大型历史快照

构建期
  事实层 → 原子 snapshot → API/搜索/榜单分片 → 上传 → 切换 manifest
```

对象路径使用内容哈希并设置一年 immutable 缓存；顶层 snapshot manifest 使用短缓存和 ETag。浏览器永不请求 GitHub raw，也不在运行时访问上游网站。

### 8.2 搜索和页面性能预算

当前首屏搜索需要约 2.4 MB 的 FIDE bootstrap，并后台再取 4.6 MB domestic bootstrap；gzip 后约 375 KB + 476 KB，尚可但会随数据增长线性恶化。建议改成：

- 首屏 JS + CSS 压缩后 ≤200 KB；
- 首次可搜索数据 ≤250 KB；
- 中文姓氏/拼音前缀/FIDE ID 分片，查询时最多追加 1–2 个分片；
- 棋手摘要 ≤30 KB，详细赛事和对局按需加载；
- 赛事目录按年份/系列分片，不再加载全量 12 MB `events.json`；
- PGN viewer 只在用户点击对局后加载；单次 PGN 包超过 2 MB 时按赛事或时间分页；
- 慢网/失败时保留最后一次成功搜索索引，并给出明确的重试和离线状态。

### 8.3 国内访问

使用自有域名，不把 `pages.dev` 作为用户品牌入口。上线前对中国大陆多个运营商和地区持续测量 DNS、TLS、TTFB、首屏和静态资源成功率；仅凭维护者本地可访问不能代表家长可访问。

若大陆访问是核心目标，应准备合规的大陆 CDN/备案方案；未完成前至少提供香港/新加坡低延迟节点和可切换镜像域名。镜像必须消费同一 `snapshotId`，不能出现数据分叉。所有字体、脚本和棋盘资源本地托管，避免 Google Fonts、国外 npm CDN 等额外单点。

### 8.4 API v2

保留 v1 兼容期，但新产品使用按资源分片的 v2：

```text
/api/v2/manifest.json
/api/v2/search/{prefix}.json
/api/v2/players/{personID}/summary.json
/api/v2/players/{personID}/events/{page}.json
/api/v2/events/{eventID}/summary.json
/api/v2/events/{eventID}/rounds/{round}.json
/api/v2/rankings/official/{basisDate}/{control}/{cohort}.json
/api/v2/rankings/reference/{modelVersion}/{basisDate}/{control}/{cohort}.json
/api/v2/estimates/{modelVersion}/{personID}.json
```

所有响应带 `schemaVersion`、`snapshotId`、`generatedAt` 和许可信息。Chess-Results 公共投影不带来源字段；含 Lichess 数据的响应必须携带适用的 CC BY-SA 许可和署名。

## 9. 现有代码深度 Review 与减法建议

### 9.1 P0：先修正确性，不做新页面

| 问题 | 证据 | 建议 |
| --- | --- | --- |
| 赛事完整性在 PGN 状态计算前被确认 | `sync_chess_results_event.py` 先生成 `complete_ids`，PGN 阶段不完整后仍 rebuild | 让 `CompletenessReport` 产生最终 `publishable_ids`；删除旧 `complete_ids` 作为发布门禁的语义 |
| PGN 缺口被粗略混成抓取失败 | 924 场含配对赛事中 742 场零 PGN 标记、179 场部分标记；当前 0 场全匹配又使用全部配对作分母 | 排除轮空，分别记录来源承诺范围和全台覆盖；前台区分“未公开棋谱/直播台棋谱完整/全台棋谱完整/抓取失败” |
| 公共对象泄露来源 | `docs/app.js`、`public-events.json`、player/event index | 构建内部审计 view 和 public view；CI 扫描公共产物禁用字段 |
| 非原子构建 | 多个 manifest 时间戳和总量不一致 | 单 snapshot 构建、验证、上传、切换；失败保持旧快照 |
| 国内身份层未消费新赛事 | domestic 全部单 sighting、0 rank/score、0 links；现有晋级计算只能在链接完成后运行 | 从 `Entry/Standing/Pairing` 重建 observation；把同俱乐部、≥65% 后升组等弱证据用于候选排序，仍不自动合并 |
| 同赛事重复 | 棋手事件索引可同时出现 slug ID 和 TNR ID、相同 PGN SHA | 先 canonical event 去重，再生成 by-player；同指纹棋局只保留一个 Game |

### 9.2 前端可以直接做的减法

`docs/app.js` 已有 2,438 行，`styles.css` 1,715 行。首页已改成搜索页，但单文件仍保留旧 dashboard/排行榜实现：

- `mergePlayers`、`mergeDomesticPlayers` 只有定义没有调用，可直接删除；
- `renderDashboard` 只有定义没有调用，首页也没有对应节点，可删除；
- `renderLeaderboardArea`、`renderTabs`、`renderLeaderboards`、`leaderboardCard` 属于已迁移到 `leaderboards.js` 的旧实现，可删除；
- 与上述功能对应的 `state.activeStage`、`ADULT_GROUPS`、DOM 引用和旧 CSS 在确认无引用后一起删除；
- `defaultSearchSuggestions` 中重复的同一条件判断应删除；
- `events.html` 的 100 多行内联脚本应迁到独立模块，与 app 共用路由、escape、筛选和错误组件；
- PGN 解析/修复、viewer、搜索、棋手页、赛事页拆成小模块，但无需为了拆分引入 React/Vue 等框架。

目标不是“工程化而工程化”，而是让每个页面只下载自己需要的代码：搜索壳、棋手详情、赛事详情、棋盘 viewer 四个边界已经足够。

### 9.3 建议删除或归档的文件/机制

以下是建议清单，不代表本次执行删除：

| 对象 | 建议 | 理由/前置条件 |
| --- | --- | --- |
| `贡献工具-双击启动.command`、`.bat` | **立即删除或改成纯说明** | 指向已不存在的 `Scripts/contrib/contrib_tool.py`，当前双击必失败 |
| 根目录旧 `一键抓取.app` | **删除旧副本，只保留现行面板入口** | 与 `一键抓取面板.command` / `Scripts/local/panel.py` 重复，容易走错入口 |
| `local-data-center/` 的 capture/tasks UI | **迁移必要审计后删除** | 与现行 panel.py 形成第二套控制面；其中日志、run-state 应在仓库外 |
| `Scripts/local/targeted_capture_panel.py`、`targeted_series_capture.py` | **并入 panel/event-queue 后删除** | 两套队列、断点和状态语言增加维护成本；须先迁移仍有价值的批次恢复测试 |
| `Scripts/build_local_data_center.py` | **拆出 CompletenessReport 构建器后删除 UI 构建部分** | 严格完整度逻辑有价值，但不应复制第二套数据中心 |
| `data/generated/chess-results-player-name-map.csv` | **迁移有效候选后删除/移出公共机器层** | 含赛事标题、尾逗号等脏姓名，不能继续成为派生输入 |
| `docs/data/youth-leaderboards.json` | **v1 客户端迁移后删除** | `leaderboards.json` 已是泛化替代，继续双写会产生漂移 |
| `crawl_player_events.py`、`fetch_event_pgn.py` 的历史独立入口 | **保留被 event-queue 调用的库代码，删除公开独立工作流** | 所有来源访问必须只有 `refresh.sh` 安全入口 |
| `publish_data_via_api.py` 的旧任意路径语义 | **彻底删除兼容分支** | 数据发布只允许不可变 outbox + manifest；保留单一 delivery 语义 |
| 根目录 `PM_REVIEW*.md`、`产品Review报告.md`、`项目评估报告.md`、旧计划 | **提取有效决策后归档或删除** | 多份文档仍写 link-only、旧数据量或已完成阶段，不能并列充当现行方案 |
| `docs/NAME_MAPPING.md` 现行版本 | **重写后替换，不保留旧口径** | 仍暗示无 FIDE 行可写入 player-aliases，与 domestic/IdentityLink 机制冲突 |
| `local-data-center/REVIEW.md` | **改为每次构建生成且不入库，或删除** | 当前仍记录 72 场详情，已被 933 场现状淘汰 |

### 9.4 不建议删除的“复杂度”

- registry authority validator、强制勘误层和错误别名清理；这是项目最重要的防腐层。
- staging、manifest、outbox、云端 ingest/rebuild/deploy receipt；流程长但解决了真实的本地来源访问与发布一致性问题。
- 身份候选队列、硬冲突和人工确认边界；未成年人同名场景不能靠“智能合并”简化。三人以上同名簇仍不得自动合并，但旧的“只生成 parent、完全不提示候选边”应被可解释的互斥方案取代。
- Lichess 许可与分片 manifest；这不是冗余元数据，而是继续分发数据的条件。

## 10. 实施路线图

### Phase 0：建立可信基线（1–2 周）

- 冻结新功能，先把当前工作树形成一次可验证的 snapshot 清单。
- 修正 PGN 前置门禁，生成逐赛事 CompletenessReport。
- 按 `not-published / advertised-partial / advertised-full / fetch-failed / locally-recoverable` 重算 PGN 状态；把盐城快棋赛列为首批离线恢复/人工补件样例。
- 公共投影删除 Chess-Results 来源字段和外链；Lichess 许可保持。
- 把当前 884 个“结构 complete”重新计算为 results、PGN availability、advertised/full-board archive、identity、publishable 多维状态。
- 消除 by-player 中 canonical event 和 PGN 指纹重复。
- 产出数据质量总表，不要求立即补齐，但禁止继续误标。

验收：公共完整赛事数可由 report 逐场复算；所有页面和 API 引用同一 `snapshotId`；CI 能阻止来源字段和 registry 覆盖回归。

### Phase 1：事实层和易补充闭环（2–4 周）

- 建立 PersonObservation、Entry、Standing、Pairing、Game、IdentityLink 规范投影。
- 用新增棋协大师赛数据重建 domestic sightings、rank、score 和跨赛事候选。
- 上线弱证据候选卡：同俱乐部、相邻组别、低组得分率 ≥65%、时间/年龄连续性及硬冲突；支持确认、保持分离和负链接 tombstone。
- 把中文名候选分成 quarantine/reviewed/corrected，清理旧脏 map。
- 统一 panel、队列、完整性和回执，移除第二套 local-data-center 控制面。
- 补月度 RatingObservation 存档。

验收：同一赛事的名单、排名、配对和 PGN 可通过自然键闭环；国内实体可出现多次赛事观察；任何身份合并都有人工证据和可撤销记录。

### Phase 2：轻量网络产品（2–3 周）

- 大型 PGN/bulk 迁对象存储，站点壳和小 JSON 保持静态。
- 搜索、赛事目录和档案改分片按需加载。
- 预生成稳定棋手/赛事 URL；未认领低龄实体默认 `noindex`。
- 建立大陆多地区可用性与性能监控、镜像切换方案。
- 发布 API v2，v1 进入有期限的兼容模式。

验收：慢速移动网络可在 3 秒内开始搜索；单一棋手档案不拉取全量赛事目录；外链打开后可以稳定恢复页面状态。

### Phase 3：估分和同龄位置（3–5 周）

- v0 先发布模型就绪度，不发布数字。
- 建立官方等级分历史、时间控制分类和有效对局图。
- 训练并时间外回测 v1；发布模型卡、版本和再现脚本。
- 达闸门后小范围上线参考估分、出生年榜和自然年龄段预测区间。
- 提供隐藏估分、匿名化和纠错入口。

验收：官方榜与参考榜数据、视觉和 URL 永久分离；每个预测名次都能追溯到参照池、snapshot 和 modelVersion；未达门槛者没有点估计。

## 11. 核心指标

### 数据正确性

- registry authority mismatch = 0；已钉死错名回归 = 0。
- `archived-advertised-complete` 赛事中，承诺 PGN—配对匹配率 = 100%；`archived-full-board` 赛事中，全部非轮空配对匹配率 = 100%。
- canonical event 重复率和 PGN 指纹重复率 = 0。
- 公共 Chess-Results 禁用字段出现数 = 0；Lichess 许可缺失数 = 0。
- 单次发布 snapshot 不一致数 = 0。

### 覆盖和补充效率

- registry 中文名覆盖率、出生年覆盖率。
- 国内 sighting 合并后平均每实体赛事数；高/中弱证据候选的审核通过率、误报率、处理时长和重复建议率。
- 棋协大师赛、李成智杯按“目标场次 / results-complete / PGN 未发布 / 直播台归档完成 / 全台归档完成 / 抓取失败 / 待人工补件”分层覆盖。
- 每新增一场完整赛事带来的新棋手、新身份连接、新可估棋手数。

### 产品体验

- 搜索可交互时间、查询零结果率、同名消歧成功率。
- 棋手档案 → 对局播放率、赛事页 → 棋手档案跳转率。
- 数据缺口提案完成率和有效率。
- 中国大陆分地区静态资源成功率、P75 TTFB/LCP。

### 模型质量

- 回测 MAE/RMSE、概率 log loss、区间覆盖率。
- 各年龄/性别/地区/样本量分层误差。
- 达到发布门槛的无官方分棋手数，而不是“尽可能多人有估分”。
- 估分纠错/隐藏请求率和模型版本回滚次数。

## 12. 最终取舍

最优路线不是重新做一个庞大的 Web 平台，而是把已经取得的数据成果变成一套可信产品：

- 数据层坚持 registry 权威、观察与身份分离、完整性可计算；
- PGN 层坚持未发布、部分直播、抓取失败和全台归档分别表达，并给人工补件明确入口；
- 身份层允许机器主动提出可解释的弱证据候选，但合并决定始终由人工确认；
- 模型层坚持官方与估算分离、区间优先、回测先于发布；
- 产品层坚持搜索优先、档案闭环、同龄位置可解释；
- 工程层坚持静态优先、按需分片、对象存储承载大文件；
- 维护层坚持一个面板、一条队列、一套状态、一份现行方案。

完成 Phase 0 和 Phase 1 后，现有 114.6 万盘授权棋谱、11,646 人注册表、14,676 条国内身份观察和 933 场结构化赛事才会真正组合成“完整的中国国际象棋棋手数据库”；估分与出生年排名应建立在这个组合完成之后，而不是替代它。
