# 五项评审优化实施记录（2026-09-13）

对应 `REVIEW_2026-09-11_FULL_PROJECT.md` 的 P1-03、P1-04、P2-01、P2-02、P2-03。
五项优化均已实施并完成云端发布、线上正文校验。最终验收完成于北京时间 2026-09-14。

## 实施结果

| 编号 | 实施内容 | 行为与边界 |
|---|---|---|
| P1-03 | 广播投影按完整 registry 集合筛选；新增 U6、unknown-age；保留转出联邦成员；逐条记录 PGN SHA-256 | 显式身份冲突与姓名歧义拒绝；无出生年不再被排除；不同棋手按自身年龄归档 |
| P1-04 | scout/promote/sync_static 的独立 CLI 阻断，旧文档改为迁移说明；reconcile 只保留离线审计 | 必需的解析库仍可导入，过时命令在来源访问和产物写入前退出 |
| P2-01 | 共享 HTML 表单解析/下载辅助函数；抽出 R2 对象事务；PGN 预建精确键、哈希、赛事日期桶索引 | 保留不可变对象冲突、全量 inventory、正文回读、配额与轮换抽验；歧义索引不再任取第一局 |
| P2-02 | 公共 HTML 发布、隐私校验和文案测试共用清单；本地估分实验移至 experiments/estimated-ratings | 原未跟踪实验与报告按 SHA-256 验证原样保留，不擅自纳入提交；实验测试显式运行，不影响生产发现 |
| P2-03 | 观察记录 schema v3 覆盖含 FIDE ID 的名单行；构建步骤 required；快照 v6 区分输入/输出；赛事 PGN 内容寻址及认证；共享协议 v1 fixture | 不新增重复国内身份；缺必要构建器即中止；赛事新对象和 receipt 未就绪时禁止部署；API 逻辑 URL 缓存 60 秒，旧版本请求返回 409 |

必要步骤门禁、事件 PGN R2 认证与线上 receipt 哈希要求已写入 AGENTS.md 和维护手册。
本次没有访问 FIDE / Chess-Results 来源，也没有改变 Cloudflare shadow 免费配额或生产入口。

## 本地验证

- 全量 Python：424 项通过（2 项按原条件跳过）；包含管线、parser、文档及新增边界回归。
- Worker：10 项通过；Python 与 JavaScript 读取同一份版本化协议 fixture。
- 赛事 PGN API：GET/HEAD、不可变 URL、非法映射、长度不符、旧版本 409 等回归通过。
- compileall、refresh.sh shell 语法及 git diff --check 通过。
- 初始兼容性基线保留原 76,605 局并恢复 570 局。随后的显式身份审计发现历史错标，故该基线只用于定位差异，不作为最终正确性数字；最终以逐局身份审计和线上回执为准。
- 原未跟踪估分实验 11 项测试通过，文件迁移前后 SHA-256 一致。
- 完整离线快照：29 个必要步骤均执行，407 个 manifest 一致性通过；178 个赛事 PGN 对象、27,018,799 字节；观察记录 73,233 条。
- 相同输入重复构建事实 SHA-256 均为 `2842d5812fc2ccf8883f7bcc106e8bcf64b659e967eeeee30734a17a0f84b52b`。
- 使用正式站点组装脚本：2,350 个文件、446,440 KiB，HTML/Markdown 清单及隐私检查通过。

不以测试通过代替数据对比。历史广播索引缺少棋局哈希，同日重复交手可能共享旧匹配键；因此先从规范 PGN 的身份信息独立建事实，再使用唯一索引补充出处。月度重投影已为全部 56,137 条索引记录补写精确哈希。

## 发布验证

首轮云端链成功：月度 34754892731、重建 34755357232、部署 34755910493。
逐局对比发现首轮移除了 896 局旧广播事实，其中 880 局的原始 PGN 明确给出不同于旧关联的 FIDE ID，是旧同名回退造成的错误关联；例如 Han, Yichen 的 PGN ID 为 1054430，旧记录却关联到 8639450。不能为维持总数保留此类错标。
另外 16 局使用 `-` 等占位符表示缺失 ID，已修正为只把正整数视为显式身份，允许缺失身份时使用唯一姓名回退。事实构建层同步拒绝显式 ID 与姓名相矛盾的关联；补充回归通过。第二轮只重新验证已有 R2 月片与投影，不回抓 Chess-Results/FIDE。
赛事映射不存在时也已取消旧可变路径兼容回退，以保持认证对象契约。最终回执见下节。

### 补充身份与观察层审计

对同一旧投影应用严格身份规则后，移除 955 条旧棋手关联，全部逐条核对为 PGN 正整数 FIDE ID 与原关联 ID 不同；没有无法解释的关联丢失。其中 880 局不再含合法 registry 关联，其余仍保留正确一方，月度原档始终保留。

人员观察层由 30,890 条增至 73,233 条，旧 observation key 无丢失，新集合中 41,081 条含 FIDE ID。registry 主档在优化前后 blob 均为 `8950612d68aa6447c3ac7fa2a642009ec49a3445`。

赛事接口实测样本 tnr1073805：不可变 R2 对象和正常 API 路径正文均为 23,136 字节，SHA-256 `0dcd8139adec3ffd1448bb80432cdc302972ab7142cf46857d9e8ad186f7c8fb`；MIME 为 application/x-chess-pgn，API max-age=60，完整哈希 ETag，过时 sha 请求返回 409/no-store。

## 最终验收（2026-09-14）

- CI [34789456567](https://github.com/keluoke/china-chess-player-pgn/actions/runs/34789456567)：成功。
- 月度维护 [34789485132](https://github.com/keluoke/china-chess-player-pgn/actions/runs/34789485132)：成功。
- 统一重建 [34789951331](https://github.com/keluoke/china-chess-player-pgn/actions/runs/34789951331)：成功，29 个必要步骤全部执行。
- 部署 [34790335793](https://github.com/keluoke/china-chess-player-pgn/actions/runs/34790335793)：成功。
- 实际部署提交：`4467ae31545733b3b427a82012ed155f1cf44aa5`。
- 快照：`20260913T233243Z-8b9ffff1`；输入提交：`83b20a4f015097068573809d1484351e11f6824c`。

80 个月片、1,235,275 条原始广播记录，所有月片 SHA-256 与优化前一致。新广播投影为 46,916 局、2,255 人；unknown-age 覆盖 13 位无出生年棋手、283 局；U6 覆盖 21 人、178 局。16 局占位符误排除已全部按棋局哈希和棋手 ID 验证恢复。

最终规范棋局事实为 76,629 局、80,271 个棋手关联：相较原基线增加 904 局、清除 880 局仅有错误身份关联的记录。移除的 955 条旧棋手关联全部有显式 FIDE ID 冲突证据；无法解释的旧棋局/关联丢失均为 0。此处统计规范事实，不把原始广播记录数等同于去重后全站局数，也不宣称所有赛事全台棋谱完整。

R2 认证覆盖 7,640 个棋手包（轮换抽验 384 个）及 178 个赛事包（全部回读），预算内完成；deploy 再次验证同一回执。正常线上 URL 下载的 snapshot、赛事对象映射、两类回执与部署提交逐字节一致：

| 线上对象 | SHA-256 |
|---|---|
| data/snapshot.json | d311b492df5c5016c0dcbc752f9efcf532bbfbafc0647e41bd0140a056980e59 |
| data/index/event-pgn-objects.json | 64a4a33df560e47ce7433ea49bda5545fbb1bf10819e925a338fd4a8121c369c |
| data/index/event-pgn-r2-receipt.json | 576f9dbc7267327aef19c8b9c6df6675590f5099f93696c818344d83f15d2981 |
| data/index/player-pgn-r2-receipt.json | ff432aa05f63253f4496ab32c9319c96dd4d904e10a249953b11e4aa1576664f |

正常线上棋手 bucket 中，8602620 提供 all 与 unknown-age 两个包；实测 unknown-age PGN 579,361 字节，SHA-256 `889f5a2349a236856a8bc71379795c31af9951f27beddb4b61747e1a7b6f4843`，与包元数据一致。最终部署后再次核对赛事 tnr1073805 的正常 API 路径，正文哈希、MIME、缓存与 ETag 均通过。

本次未改动注册表身份、人工赛事控制 CSV 或采集 outbox。已通过受控 runtime plan/sync 同步已提交代码；既有未跟踪实验文件保留在独立实验目录。
