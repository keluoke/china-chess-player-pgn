# UI/交互体验 Review + 无 FIDE 棋手估分机制方案(2026-07-12)

> 对象:main @ 6b63fef0(搜索优先改版 4 个提交)。只评审、只给方案,不改代码。
> 第一部分:UI 界面与交互体验完整报告;第二部分:无 FIDE 分棋手的估分机制可行性与设计。

---

# 第一部分 UI 与交互体验报告

## 一、改版总评

这次改版几乎是对上一份 UX review 的逐条实现,且多处做得比建议更完整:首页彻底重构为搜索优先(大标题 + 引导语 + 示例/最近搜索建议);排行榜和覆盖页拆为独立页面并挂顶部导航;搜索加了 180ms 防抖 + 中文输入法组字保护(compositionstart/end)、两字符起搜门槛(纯数字例外,FIDE ID 可搜)、截断透明提示("仅显示前 30 条,请补充拼音…")、匹配词高亮 `<mark>`、"找到 N 位棋手(M 个姓名待区分)"的统一口径、按查询形态动态决定赛事/棋手先后;历史栈用 pushState + 快照恢复(含滚动位置),返回键不再把人送出网站;搜索词进 URL `?q=`,搜索状态本身可分享;棋手页补上了分享按钮(navigator.share + 剪贴板降级)、赛事记录按日期倒序 + `<details>` 展开其余项、名次加粗提前、"bulk 命中"改为"已收录 N 盘青少年赛事对局"+ 年龄组友好标签、棋盘快捷键提示文案、FIDE 棋手页也有了删除/匿名化入口。加载体验也对了:搜索框在数据就绪前 disabled 且 placeholder 写明"正在加载 2.6 万名棋手…"。

**方向全部正确。本轮发现 1 个高优先级的架构性回退、1 个由来已久的数据拖累,以及若干打磨项。**

## 二、新发现的问题

### U1(高)搜索结果点击从"页内切换"变成了"整页刷新"——最热路径变重了

结果项从 `<button>` 改成了真实链接 `<a href="?fideID=…">`(app.js:667、712),但**没有拦截同源点击做 pushState 页内切换**——searchResults 的 click 监听只做了 rememberSearch(app.js:252-256)。后果:点击任何搜索结果都触发整页重载,重新初始化整个应用并重新 fetch 8.9MB 的 search-bootstrap.json(有 HTTP 缓存兜底,但手机上 JSON 重新解析 + JS 重新初始化的代价仍在,弱缓存环境下是重新下载);且 href 不携带 `?q=`,前进后再返回依赖历史条目里的旧 URL。改成 `<a>` 的收益是真实的(可中键新开、可长按分享、无障碍、爬虫可见),**正确形态是两者兼得**:保留 href,同时拦截左键单击走页内 selectPlayer(渐进增强的标准做法)。当前状态等于把上一轮修好的 SPA 流畅性在最高频交互上又交了回去。

### U2(高)8.9MB 的启动包:加载态诚实了,但包本身还没瘦

`build_search_bootstrap.py` 把此前分散的 10 个请求合并成单一 bootstrap 文件,是正确的第一步;但它仍然装着全量 2.6 万实体的全部别名,未压缩 8.9MB(Cloudflare brotli 后估计 2–3MB)。4G 手机上这仍是数秒到十几秒的"搜索框禁用期",而首页现在**只有**搜索——禁用期内整个产品不可用。建议下一步:bootstrap 只保留搜索必需字段并做前缀分片(按拼音首字母/姓氏分片,首包 <1MB,输入时按需取片);或首包只含"有对局/有中文名/排行榜内"的高热实体(约 5 千),长尾实体查询时回退到分片。配套:给 bootstrap 加内容哈希文件名 + immutable 缓存头,让 U1 的整页刷新至少稳定命中缓存。

### U3(中)消歧卡的区分特征仍是空的——数据欠账拖累了三轮 UI

消歧卡无 FIDE 分支的上下文依赖 `player.publicLocation`(app.js:709),该字段在公开数据中仍为 0 条(domestic registry 未按新脚本重建);同时完整 `club` 仍在公开 JSON 里(隐私问题,PM_REVIEW_2 N1)。本轮把 identity-review.json 挡在部署外了(prepare-static-site --exclude),说明意识到了,但主矛盾未动。**UI 已经三轮迭代到位,就等这一次数据重建**:publicLocation 落地 + club 出公开面,消歧体验和隐私合规一次解决。

### U4(中)首页"极简"过头,失去了可信度信号

旧首页的数据看板(收录棋局数、最新赛事、更新记录、鸣谢)整体消失,新首页只有搜索框和三个示例词。搜索优先是对的,但**空首页对首次访客缺少"这个库有多少货、多新鲜"的信任线索**——家长决定要不要认真搜的依据。建议在搜索框下方加一行轻量状态("收录 72,610 盘对局 · 覆盖 21,091 项赛事 · 更新于 7 月 12 日",点击进覆盖页),不恢复大看板。

### U5(中)搜索键盘导航只做了一半

输入框 ↓/Enter 聚焦或选中首条、结果内 ↑↓ 移动(app.js:257-263,408-415)都有了,但:Esc 不能清空查询/关闭结果;结果内按 Enter 依赖原生链接行为(会触发 U1 的整页刷新);↑↓ 移动跳过了消歧卡内部的子选项与"登记缺口"按钮;焦点样式未确认(styles 中未见 :focus-visible 定制)。可用,但盲打流未闭环。

### U6(低)细节清单

排行榜页每组只显示前 20 且无"查看更多",totalEligible 显示 740 人但看不到 21 名以后;排行榜行点击跳回首页 `?fideID=`(又是整页跳转,同 U1 一并处理);`?q=` 恢复时搜索建议与结果同时渲染,视觉略挤;示例词硬编码("丁天一/侯逸凡/李成智杯")建议由构建期从热门数据生成;分享文案未带成绩摘要(建议"××,U12,标准棋 1580,已收录 34 盘对局"提高群转发点开率);仍无站点访问统计——本轮这么大的 UI 改版,上线后无法度量任何行为变化,改版效果全靠感觉。

## 三、上一轮遗留事项状态

| 事项 | 状态 |
|---|---|
| S1 历史栈 / S2 加载反馈 / S3 截断透明 / S4 搜索不吞详情页 / S5 排序与高亮 / S6 防抖・IME・最近搜索・示例・两字符门槛 / S7 口径统一 | ✅ 全部实现 |
| P1 赛事记录展开 / P2 分享按钮 / P3 家长文案 / P5 快捷键提示 / P7 隐私入口对齐 | ✅ 全部实现 |
| P4 等级分走势("较上月±N"起步) | ❌ 未动,维持建议 |
| P6 "这是我的孩子"认领入口 | ❌ 未动;消歧卡只有"逐条核对"没有认领动作,身份合并管线仍无家长入口(identity links 仍 0 条) |
| 隐私:club 出公开面 + publicLocation 重建 | ❌ 未动(见 U3) |
| SEO/OG/sitemap、自定义域名、访问统计 | ❌ 未动;首页改成纯客户端搜索后,SEO 缺失的机会成本更大了 |

## 四、优先级

| 级别 | 事项 |
|---|---|
| 高 | U1 拦截结果链接做页内切换(保留 href) |
| 高 | U3 重建 domestic 数据:publicLocation 落地 + club 出公开面(一次解决消歧与隐私) |
| 高 | U2 bootstrap 分片瘦身 + 不可变缓存 |
| 中 | U4 首页信任状态行;U5 键盘流闭环;P6 认领入口;访问统计 |
| 低 | U6 细节清单;P4 等级分走势 |

---

# 第二部分 无 FIDE 分棋手的估分机制:可行性与方案

## 一、结论先行

**方法上完全可行,且有成熟先例;但以今天库里的数据,一个字都算不出来。**估分需要"谁对谁、什么结果"的对局级数据,而当前:14,676 条 sighting 的 `rank`/`score`/`birth_year` 字段填充率全部为 0(只有名单,没有成绩);逐轮成绩表 `domestic-event-round-results.csv` 为空表;赛事详情(含逐轮对阵+结果+双方身份)只入库了 1 个赛事(tnr1429695)。**估分机制的先决条件恰好就是已排入队列、尚未执行的 201 个赛事整取目标**——这不是新工程,是给既有管线一个新的、对家长极有吸引力的产出。

## 二、数据基础盘点

| 资产 | 现状 | 对估分的作用 |
|---|---|---|
| FIDE 注册表 | 11,623 人,含 standard/rapid/blitz 与出生年(97%) | **锚点集**:把估分钉在 FIDE 标尺上 |
| 赛事详情(整取产物) | 逐轮对阵含双方 fideID/中文名、结果、棋谱链接——格式完美,**但只有 1/201 个赛事** | 估分的核心原料 |
| sightings | 14,676 条,仅名单(rank/score 全空) | 只能建身份,不能算分 |
| PGN 档案 | 72,610 盘(FIDE 棋手为主) | 补充结果对,头部含部分对手等级分 |
| 年龄组排行榜 | U8 有分仅 24 人、U10 仅 204 人 | 分位参照系;同时暴露低龄锚点稀薄的难点 |

## 三、机制设计

### 3.1 模型:锚定池 Elo(推荐),表现分兜底

**L0 · 赛内表现分(v0,单赛事)**:某组别中有分对手占比足够时,按 FIDE 表现分口径(对手平均分 + 得分率查 dp 表)给出单赛事表现区间。可解释、可人工复核,家长看得懂("这次比赛下出了约 1400 的水平")。缺点:单赛噪声大,无分对手的对局被浪费。

**L1 · 全库锚定 MLE(v1,推荐主模型)**:把全部对局结果建成图,有 FIDE 分的棋手固定为锚(分数不动),对无分棋手做 Bradley-Terry/Elo 最大似然迭代求解。关键优势:**无分 vs 无分的对局也参与传导**——低龄组孩子之间的比赛,通过少数"桥"(既打低龄组又碰有分对手的孩子)连接到锚集,这正是 Chessmetrics、各国棋协(如 EGF)处理历史/无分人群的标准做法。纯离线批计算,与"构建期生成、零运行时"架构完全兼容。

**L2 · 不确定度**:对 L1 结果给出后验区间(Glicko 式 RD 或自助法),对外只发布"估分 ± 区间",如 **"约 1250(±120)"**。区间同时是发布门槛的量化依据。

### 3.2 发布门槛(没有达标,宁可不显示)

估分只在同时满足以下条件时对外展示:① 该实体 ≥9 盘可用结果(FIDE 定 initial rating 的最低口径);② 结果图上到锚集 ≤2 跳;③ ≥5 名不同对手;④ 区间宽度 ≤±200;⑤ **身份为已核验或已合并实体**——分裂的同名实体各自凑不够对局,估不出分。条件 ⑤ 让估分与认领/身份合并形成正循环:**想看孩子的估分?先认领并合并他的档案。**这可能是比"成长档案"更强的认领动机。

### 3.3 年龄段位置的呈现

不给精确名次,给**分位带**:"按估分,约在 U10 组(有分+可估棋手共 N 人)的前 25–40%"。理由:估分本身有区间,精确排名是伪精度;且对儿童,"第 187 名"的挫败感远大于"前三分之一"的信息量。参照系 = 该年龄组 FIDE 有分棋手 + 通过门槛的可估棋手的合并分布,两类人显式标注。

### 3.4 产品红线

估分**永不进入 FIDE 排行榜**(排行榜是官方数据的呈现,混入即失信);棋手页单独"参考估分"模块,灰色"估算"徽标 + 一句话依据("基于 23 盘对 15 名有分对手的成绩")+ 指向方法学文档的链接(公开算法、版本号、每次重建可复现);措辞永远向上兼容("约在前 25–40%"而非"仅排第 N"),不做贬损性表达;估分是未成年人的**派生个人信息**,与档案一样接受删除/匿名化请求,家长可选择隐藏该模块。

### 3.5 已知偏差与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 低龄组图不连通 | U8 全场无分锚点(有分者仅 24 人),部分组别到不了锚集 | 显示"证据不足,再收录 N 盘可估",把缺口转成家长贡献动机;快棋分可作次级锚 |
| 青少年 FIDE 分滞后 | 涨棋快的孩子官方分低于真实水平,锚本身有偏 | 锚不使用超过 12 个月未参赛者;文档明示"以 FIDE 标尺为准,系统性偏保守" |
| 选择偏差 | 库里只有被收录赛事,强弱对手非随机 | 对手多样性门槛(3.2③)+ 区间发布 |
| 身份错合 | 合错人=两人的对局混在一起,估分立刻失真 | 只用已核验/已合并实体(3.2⑤);错合走既有勘误回滚 |

### 3.6 实施路径(与现有管线的依赖关系)

```
前置   消化赛事整取队列(201 个目标)→ 逐轮对阵数据从 1 个赛事变成一批
v0     单赛事表现分:整取一个赛事即可给该赛事全部无分棋手算表现区间(先李成智杯一届试点)
v1     全库锚定 MLE + 区间 + 发布门槛,棋手页上线"参考估分"模块与年龄段分位带
v2     时间衰减与成长曲线(估分随赛事时间序列变化,接入"等级分走势"呈现)
```

v0 在第一批赛事整取完成后即可交付,是"抓取→家长可感知价值"链条上最短的一环;它同时给 201 个赛事的抓取排序提供了新依据——**优先抓有分/无分棋手混编的组别,每抓一个,一批孩子的估分就点亮一批**。

## 附:两部分的交汇点

第一部分的 P6(认领入口)和第二部分的门槛⑤(仅对已合并实体估分)是同一件事的两面。建议的产品叙事:搜索到孩子 → 认领合并档案 → 解锁"参考估分与年龄段位置" → 生成可分享的成长卡片。这条链把本项目最难的数据问题(身份合并)、最强的用户动机(我孩子什么水平)和唯一的增长通道(家长群分享)串在了一条线上。
